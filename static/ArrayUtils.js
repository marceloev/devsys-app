export default {

    install(Vue, options) {

        /*Vue.prototype.$isInArray = (array, elem) => isInArray(array, elem);

        function isInArray(array, elem) {
            console.log(array);
        };*/
    }
}